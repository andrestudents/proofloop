{
  "name": "proofloop",
  "version": "1.0.0",
  "private": true,
  "description": "Live window into an agent build-verify loop (Claude Code + Kane CLI)",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "tsx watch src/server.ts",
    "test": "tsx --test tests/kaneParser.test.ts tests/agentStream.test.ts tests/runManager.test.ts"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
